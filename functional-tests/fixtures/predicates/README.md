These predicates are the serialized `bip340-schnorr-test` values previously
derived by datatool from the native test signing keys in the proof programs:
checkpoint uses `[0x01; 32]`, and Alpen account uses `[0x02; 32]`.
