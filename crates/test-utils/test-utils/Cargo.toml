[package]
edition = "2021"
name = "strata-test-utils"
version = "0.4.0"

[lints]
workspace = true

[dependencies]
arbitrary.workspace = true
rand_core = { workspace = true, features = ["getrandom"] }
