# Sequencer compose stack — developer inputs only.
# Generated values (keys, params) live in configs/generated/.
#
# Copy to .env and edit as needed:
#   cp .env.example .env

# --- used by: gen-params-and-elfs.sh, init-network.sh, strata ---
BITCOIN_NETWORK=signet
BITCOIND_RPC_URL=http://bitcoind:38332
BITCOIND_RPC_USER=bitcoin
BITCOIND_RPC_PASSWORD=bitcoin
GENESIS_L1_HEIGHT=20
OUTPUT_DIR=./configs/generated

# --- used by: init-network.sh (params generation) ---
# P2TR BOSD descriptor for the bridge emergency sweep address (required).
SAFE_HARBOUR_ADDRESS=
BRIDGE_DENOMINATION_SATS=100000000
MAX_WITHDRAWAL_DESCRIPTOR_LEN=81
# MAX_WITHDRAWAL_AMOUNT_SATS=1000000000
# Genesis snark accounts JSON from alpen-ee; defaults to the committed dev fixture.
# GENESIS_ACCOUNTS=

# --- used by: compose-signet.yml ---
MNEMONIC=
MINERENABLED=1
MINE_GENESIS=1
BLOCKPRODUCTIONDELAY=2

# --- used by: all ---
RUST_LOG=info

# --- Uncomment for real SP1 proofs ---
# CHECKPOINT_PREDICATE=sp1-groth16
# STRATA_CARGO_FEATURES=prover,sp1
# SP1_PROVER=network
# SP1_PROOF_STRATEGY=reserved
# NETWORK_PRIVATE_KEY=0x...
# NETWORK_RPC_URL=https://rpc.production.succinct.xyz
